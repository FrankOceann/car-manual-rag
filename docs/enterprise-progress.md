# 工程化实施记录

依据：用户批准的汽车手册 RAG 下一阶段工程化计划。

| 工作 | 状态 | 约束及检查 |
|---|---|---|
| 部署、迁移、配置、备份 | 已完成隔离验证 | Docker 镜像构建、模型预下载、健康检查和新卷恢复演练均已完成；8 项备份安全测试通过 |
| 数据库、认证、导入任务 | 已完成隔离验证 | 版本隔离、幂等、失败保留旧版本；真实 PostgreSQL、Redis、Chroma、Celery 集成测试通过 |
| 前端登录、管理、引用 | 已完成本地测试 | 令牌仅内存；15 项前端测试和生产构建通过 |
| 集成、评测、恢复演练 | 已完成隔离验证 | baseline/hybrid 评测已重跑；实际 PostgreSQL、Redis、Chroma、Celery 链路、上传与恢复演练均通过 |

基线：原后端 79 项测试通过。当前离线后端测试为 95 项通过，真实服务集成测试另行通过；前端 15 项通过。
Docker 引擎经授权访问检查返回 29.7.2。

接口交叉检查：前端消费后端 auth/admin/jobs/file 接口；
部署消费 app.tasks:celery_app、Alembic、prepare_model/create_admin 命令；
实现共同保留旧问答主要字段，新增强制登录。

决策：离线评测直接使用旧 Chroma 快照；线上 API 只检索数据库激活版本。
决策：PostgreSQL 为任务状态事实来源，Redis 仅负责投递；周期扫描恢复未投递任务。

2026-09-11 验证：

- baseline：Recall@1 0.40、Recall@3 0.90、MRR 0.60；hybrid：Recall@1 0.70、Recall@3 0.80、MRR 0.75。题集是开发基准，不代表线上答案准确率。
- 修复 Chroma 连接异常包装导致的错误失败分类；临时连接、限流和 5xx 会保留为可重试任务，PDF 无文本仍立即失败。
- 修复 HTTP Chroma 客户端重复创建；API 生命周期结束时关闭共享客户端。
- 备份恢复解压显式拒绝路径穿越、链接和特殊文件，并兼容 Python 3.11；备份阶段会校验卷内链接目标并实体化为普通文件，避免模型缓存中的安全内部链接造成恢复失败。
- 已实际构建前后端镜像、预下载嵌入模型、启动 PostgreSQL/Redis/Chroma/API/Worker/beat/Nginx 并通过健康检查；浏览器登录页可访问。首次本地构建因 Docker 到官方 PyPI TLS EOF 失败，现保留官方默认源并支持单次 `PIP_INDEX_URL` 覆盖。
- 已使用自编 PDF 完成管理员登录、上传、后台导入成功、普通用户权限拒绝、原文下载及章节读取验证。DeepSeek 未在隔离演练中调用，避免使用真实密钥和产生费用。
- 已将备份恢复到新项目和新卷，恢复后账户、手册、版本数量正确，PDF SHA-256 与原项目一致；原项目卷未被覆盖。
- `npm audit` 报告 Vite/Vitest 开发工具链的上游问题；生产 Nginx 镜像仅复制静态构建产物。本阶段未升级主版本依赖，以避免无验证的框架迁移。
