# Car Manual RAG backend

FastAPI backend for the vehicle-specific manual retrieval and grounded-answering
demo. Install the project from this directory and run its test suite with
`python -m pytest -v`.
